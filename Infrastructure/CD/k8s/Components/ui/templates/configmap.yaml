apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  app:
    properties:
      app.name=Business-Inventory
      app.version=1.0.0
      log.level=INFO
  database.yml:
    host: localhost
    port: 5432
    database: inventory_db